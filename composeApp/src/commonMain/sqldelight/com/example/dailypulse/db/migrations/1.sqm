-- Migration to create Source table with all fields
CREATE TABLE Source (
    name TEXT NOT NULL PRIMARY KEY,
    url TEXT NOT NULL,
    icon TEXT NOT NULL,
    desc TEXT NOT NULL,
    category TEXT NOT NULL,
    language TEXT NOT NULL,
    country TEXT NOT NULL,
    totalArticles INTEGER NOT NULL DEFAULT 0
);

